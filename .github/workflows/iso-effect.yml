name: iso-effect
env:
  CI: true
  FORCE_COLOR: 1
on:
  push:
    branches:
      - main
    paths:
      - 'packages/iso-effect/**'
      - .github/workflows/iso-effect.yml
      - pnpm-lock.yaml
  pull_request:
    paths:
      - 'packages/iso-effect/**'
      - .github/workflows/iso-effect.yml
      - pnpm-lock.yaml
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: latest
      - uses: actions/setup-node@v7
        with:
          node-version: lts/*
          cache: pnpm
      - run: pnpm install
      - run: pnpm run build
      - run: pnpm -r --filter iso-effect run lint
      - run: pnpm -r --filter iso-effect run test
      - run: pnpm -r --filter iso-effect exec depcheck

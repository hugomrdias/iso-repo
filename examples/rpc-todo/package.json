{
  "name": "rpc-todo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "server": "node src/server.ts",
    "server:watch": "node --watch src/server.ts",
    "cli": "node src/cli.ts",
    "lint": "tsc --build && biome check --no-errors-on-unmatched --files-ignore-unknown=true ."
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "hono": "^4.12.14",
    "iso-base": "workspace:*",
    "iso-kv": "workspace:*",
    "iso-signatures": "workspace:*",
    "iso-ucan": "workspace:*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "~6.0.3"
  }
}
